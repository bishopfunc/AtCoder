#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main() {
  int N, K;
  cin >> N >> K;
  vector<int> A(N), B;
  for (int i = 0; i < N; i++) cin >> A[i];
  int idx = 0;
  for (int i = 1; i < N; i++) {
  }
}